package kingdom;

//interface that has an abstract method in it called test. This test gets it implimentation from the lambda expresson that is sent in. 
public interface CheckAnimal
{
	boolean test(Animal a);
}